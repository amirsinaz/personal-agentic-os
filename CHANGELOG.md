# Changelog

<!-- Public, user-facing changes only. Exclude review automation, release operations, private analytics, deployment work, and maintainer-only fixes. -->

## 0.9.1 — 2026-09-02

- محتوای علامت‌گذاری‌شده به‌عنوان خصوصی و الگوهای Credential پیش از ثبت در حافظه حذف می‌شوند.
- رجیستری Agentها اکنون فیلتر پروژه و نوع Agent و صفحه‌بندی پایدار دارد.
- هزینه‌ی دو بازه‌ی هم‌اندازه فقط با قیمت و مصرف تأییدشده مقایسه می‌شود و تغییر مشاهده‌شده به‌عنوان صرفه‌جویی قطعی معرفی نمی‌شود.
- اثر بهینه‌سازی تا ثبت حداقل پنج اجرای معتبر «در دسترس نیست» می‌ماند و پس از آن نیز فقط تغییر مشاهده‌شده گزارش می‌شود.
- گزارش یکپارچگی Sync پروژه‌های جدید، تغییرکرده و حذف‌شده را بدون بازنویسی خودکار داده‌ها مشخص می‌کند.

## 0.8.0 — 2026-09-01

- نصب عمومی با `npx personal-agentic-os@latest` و مسیرهای ثابت Setup، Dashboard، Sync، Update و Optimization اضافه شد.
- شناسایی معنایی پروژه‌ها و انتقال حافظه در یک مرحله‌ی جدا و نیازمند تأیید باقی ماند.

## 0.7.0 — 2026-08-29

- Release Manifest اکنون حداقل نسخهٔ پشتیبانی‌شده، شدت و سیاست به‌روزرسانی را مشخص می‌کند.
- در اصلاح‌های بحرانی، نسخه‌های پایین‌تر از حداقل مجاز هشدار «به‌روزرسانی ضروری» می‌بینند و Sync تا نصب نسخهٔ سالم متوقف می‌شود.
- Dashboard در هر بار شروع، Manifest عمومی را بدون شناسهٔ کاربر بررسی می‌کند؛ خطای شبکه مانع استفاده از داده‌های محلی نمی‌شود.
- فورس آپدیت هیچ فایل، Vault یا تنظیم شخصی را خودکار بازنویسی نمی‌کند و کاربر را به Release عمومی هدایت می‌کند.
- آمار نصب همچنان فقط برای کاربرانی ثبت می‌شود که Telemetry اختیاری را صریحاً پذیرفته‌اند.

## 0.6.0 — 2026-08-29

- قرارداد اتصال مستقل از نام ابزار اضافه شد تا ابزارهای سازگار آینده با Manifest نسخه‌دار ثبت شوند.
- اتصال جدید به‌صورت پیش‌فرض غیرفعال، با دسترسی حداقلی و سلامت نامشخص ساخته می‌شود.
- انتقال حافظه با Envelope پاک‌سازی‌شده و Revision قطعی انجام می‌شود تا ارسال مجدد باعث ثبت تکراری نشود.
- Manifestهای نامعتبر، اتصال HTTP ناامن و Capabilityهای تکراری پیش از ثبت رد می‌شوند.
- نصب Connector، فعال‌سازی شبکه، Credential و دسترسی نوشتن یا اجرای Sync همچنان تأیید جداگانه می‌خواهند.

## 0.5.0 — 2026-08-29

- هر رکورد حافظه اکنون منبع، زمان، سطح اطمینان و وضعیت تأیید دارد؛ فرضیه به‌صورت خودکار به واقعیت تبدیل نمی‌شود.
- برای هر پروژه یک Context Pack محلی و پاک‌سازی‌شده در قالب Markdown و JSON ساخته می‌شود تا انتقال حافظه به ابزار جدید ساده‌تر باشد.
- Sync برای هر پروژه Fingerprint ثبت می‌کند و تعداد موارد بررسی‌شده، تغییرکرده، واردشده و بدون تغییر را گزارش می‌دهد.
- Dashboard تعداد Context Packها و وضعیت سلامت حافظه یا موارد نیازمند بررسی را نشان می‌دهد.
- بررسی سلامت، پروژه‌های بدون Context Pack و رکوردهای تأییدنشده را بدون تغییر خودکار داده‌ها مشخص می‌کند.

## 0.4.0 — 2026-08-29

- راه‌اندازی اکنون ابزارهای هوش مصنوعی کاربر را می‌پرسد و فقط اتصال‌های تأییدشده را ثبت می‌کند.
- نسخه‌های مشترک یک پروژه با مسیر، شناسه‌ی Repository یا نشانه‌های محتوایی قوی در یک نقشه‌ی Canonical قرار می‌گیرند؛ شباهت نام به‌تنهایی باعث ادغام نمی‌شود.
- نقشه‌ی پروژه‌ها پیش از انتقال حافظه نمایش داده می‌شود و بدون تأیید کاربر هیچ فایلی ساخته نمی‌شود.
- Sync افزایشی وضعیت پروژه‌های ایجادشده، تغییرکرده، بدون تغییر یا حذف‌شده را ثبت می‌کند.
- Dashboard ابزارهای متصل، پروژه‌های مشترک و نتیجه‌ی آخرین Sync را نشان می‌دهد.
- الگوهای اختیاری همگام‌سازی مداوم برای macOS، Linux و Windows اضافه شدند.

## 0.3.1 — 2026-08-28

- بخش پیشنهادهای بهینه‌سازی در داشبورد خواناتر و فشرده‌تر شد.
- وضعیت «نیاز به بررسی» و راهنمای آن اضافه شد؛ هیچ تغییری بدون تأیید کاربر اعمال نمی‌شود.
- نمایش شواهد و پیشنهادها در موبایل اصلاح شد.

## 0.3.0 — 2026-08-28

- Added a dashboard notice when a newer version is available.
- Added an optional weekly version check for macOS, Linux, and Windows.
- Kept updates approval-based: the system shows the release first and never installs it automatically.

## 0.2.0 — 2026-08-28

- Added an evidence-backed Personal Agent Registry to the local dashboard.
- Added project-scoped profiles for primary agents and subagents.
- Preserved explicit `unknown` values instead of inferring responsibilities or skills.
- Kept private project data, transcripts, hidden reasoning, and local paths outside public releases.

## 0.1.0 — 2026-08-27

- Published the initial local Obsidian memory, usage, cost, budget, recommendation, and reversible optimization workflow.
