$action = New-ScheduledTaskAction -Execute "NODE_PATH" -Argument '"REPO_PATH/src/check-update-cli.js" "CONFIG_PATH"'
$trigger = New-ScheduledTaskTrigger -Weekly -DaysOfWeek Monday -At 10am
Register-ScheduledTask -TaskName "Personal Agentic OS update check" -Action $action -Trigger $trigger
